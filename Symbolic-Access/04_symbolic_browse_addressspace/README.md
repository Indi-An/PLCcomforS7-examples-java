# 04 – Browse PLC Address Space (Java) (works with all licenses)

This example demonstrates browsing/searching the symbolic namespace.

Use cases:
- discover DBs/structs/arrays/variables
- copy full symbolic variable names for read/write/subscription requests

## Notes
- For large projects, the initial import of symbolic metadata can take time.
